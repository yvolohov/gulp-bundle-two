class Rectangle {
  constructor(width, height) {
    this.width = width;
    this.height = height;
  }

  show() {
    console.log(`Width: ${this.width}; Height: ${this.height};`);
  }
}

export default Rectangle;
